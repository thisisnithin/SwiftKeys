mutation (
  $accuracy: Float!
  $wpm: Int!
  $correct: Int!
  $wrong: Int!
  $email: String! @fromClaim(name: EMAIL)
  $time: Int!
) {
  db_createOneResults(
    data: {
      accuracy: $accuracy
      wpm: $wpm
      correct: $correct
      wrong: $wrong
      time: $time
      User: { connect: { email: $email } }
    }
  ) {
    id
  }
}
